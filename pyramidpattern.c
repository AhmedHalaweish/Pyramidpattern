/*Written by: Ahmed Essam Taha */

//pyramids patterns, different functions for every type
/*include directives*/
#include <stdio.h>
#include "fun.h"

/*patterns call available  
<take note>/replace size with the required parameter number ex. 4/ >

void half_pyramid(signed char size);
void inv_pyramid(signed char size);
void full_pyramid1(unsigned int size);
void Inv_full_pyramid(unsigned int size);
void hollow_full_pyramid(unsigned int size);
void inv_hollow_half_pyramid(signed char size);
void inv_hollow_full_pyramid(signed char size);

*/

int main()
{
	// change function call for different patterns
	inv_hollow_full_pyramid(6);
	return 0;
}
